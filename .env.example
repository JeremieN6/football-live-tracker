VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Monitoring d'erreurs (Sentry) — optionnel, l'app tourne normalement si vide
VITE_SENTRY_DSN=
# Supabase Edge Function secret (set via `npx supabase secrets set`)
# ANTHROPIC_API_KEY=

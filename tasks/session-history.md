# Historique de session -- NRV (archive)

> Ce fichier N'EST PAS lu automatiquement (contrairement a CLAUDE.md). Il contient le journal narratif complet de chaque session de travail -- diagnostics, allers-retours avec l'utilisateur, details d'implementation. A ouvrir uniquement sur demande (ex. "qu'est-ce qu'on avait fait sur X ?"). CLAUDE.md garde une version condensee (Ce qui est fait / Decisions Prises) suffisante pour le travail courant.

---

## Notes de Session
- Phase 4 a 7 finalisees.
- Realtime ajoute dans le tracker via `useMatchSync`.
- Generation de rapport IA operationnelle via `generate-report` (Anthropic).
- Harden de securite applique : retrait des variables sensibles cote client.
- Build production valide (`npm run build` OK) et fonction Supabase redeployee.
- 11/09/2026 : Ajout effectif de club (`players`) + selection titulaires/remplacants par match (`match_lineups`), sans placement terrain. Les evenements GOAL_FOR/YELLOW_CARD/RED_CARD/SUBSTITUTION portent desormais des references vers l'effectif (scorer_id, assist_id, player_id, player_in_id, player_out_id) a la place des anciens champs texte libre player_in/player_out. ReportView affiche un rapport nominatif sans IA (buteurs, passeurs decisifs, cartons, chronologie), independant du bloc Analyse IA. Discussion sur une migration vers Neon : ecartee pour ce projet (voir Decisions Prises), l'utilisateur reste sur Supabase.
- Build (`npm run build`) et typecheck (`vue-tsc -b`) valides apres ces changements.
- 11/09/2026 : Migration `20260911120000_players_and_lineups.sql` appliquee par l'utilisateur sur le vrai projet Supabase (SQL Editor). Parcours complet teste en conditions reelles par l'utilisateur : creation joueurs dans l'effectif, nouveau match, selection titulaires/remplacants, but avec buteur + passeur decisif, carton jaune nominatif, remplacement via liste deroulante, rapport final affichant buteurs/passeurs/cartons/chronologie — tout fonctionne.
- Bug corrige en cours de test : `RosterView.vue`, le champ numero (`<input type="number">` + v-model) pouvait renvoyer une valeur de type `number` selon le navigateur au lieu d'une chaine, faisant planter `.trim()` a la creation d'un joueur ("number.value.trim is not a function"). Fix : cast explicite `String(number.value).trim()` avant conversion en `Number`.
- PR #1 (effectif/lineups/rapport nominatif) mergee dans `main`.
- 11/09/2026 (suite) : Ajout de la vue profil joueur. Point de donnees manquant identifie : le champ `minute` des evenements est relatif a la mi-temps en cours (remis a zero par `switchHalf`), et rien n'enregistrait la duree reelle de chaque mi-temps — impossible de calculer des minutes jouees fiables sans ca. Ajout de `first_half_minutes`/`second_half_minutes` sur `matches` (migration `20260911130000_match_durations.sql`), captures dans `TrackerView` (au changement de mi-temps et a la fin du match) et persistees via `matchStore.finishMatch`. Nouveau store `playerProfile.store.ts` : agrege pour un joueur donne, a partir de `match_lineups` + `events`, le nombre de matchs joues, titularisations, entrees en jeu, buts, passes decisives, cartons, minutes jouees (total + moyenne — seulement sur les matchs dont la duree est connue, avec avertissement affiche si des matchs anciens manquent cette info). Vue `PlayerProfileView.vue` sur `/roster/:id`, accessible en cliquant un joueur dans `RosterView`.
- 11/09/2026 (suite) : Discussion sur la demande "equipes + divisions + cloisonnement multi-club". Choix de poser la fondation (clubs/equipes) avant d'ajouter la feature en surface, decoupe en 3 phases (voir Decisions Prises). Implementation de la Phase A : tables `clubs`/`teams`/`club_members`, `players`/`matches` rattaches a `club_id`+`team_id`, vue `/teams`, RosterView/CreateMatchModal mis a jour, detection auto du "renfort" dans le profil joueur. Migration `20260911140000_clubs_teams.sql`.
- 11/09/2026 (suite) : Premier essai de la migration clubs/teams par l'utilisateur en echec ("column owner_id does not exist") — la table `clubs` avait ete partiellement creee lors d'une premiere tentative. Migration reecrite pour etre rejouable sans danger (colonnes/contraintes ajoutees defensivement via `add column if not exists`, policies redefinies via `drop policy if exists` + `create policy`, backfill qui ne recree pas de club si le coach en a deja un). **Rejouee avec succes par l'utilisateur** — migrations `20260911130000_match_durations.sql` et `20260911140000_clubs_teams.sql` appliquees en prod. Reste a tester en conditions reelles (creation d'une 2e equipe, filtre/tri effectif, match rattache a une equipe).
- PR #2 (profil joueur + fondation multi-club Phase A) ouverte, pas encore mergee.
- 11/09/2026 (suite) : Premier test reel de creation d'equipe echoue ("Erreur lors de l'enregistrement.", generique) sur un club affiche "Mon club" mais 0 equipe. Cause probable : un appel `ensureClub()` precedent (dans une autre vue, ex. RosterView) a cree la ligne `clubs` puis echoue avant de creer sa premiere equipe (aucun `try/catch` autour de `ensureClub()` dans les vues, donc echec silencieux cote UI), laissant un club orphelin sans equipe que les visites suivantes recuperent tel quel (la branche "club existant" de `ensureClub()` ne verifiait pas s'il avait une equipe). Bug annexe trouve en creusant : le pattern `err instanceof Error ? err.message : fallback`, duplique dans ~14 fichiers, masquait le vrai message d'erreur Supabase des que l'objet leve n'etait pas strictement reconnu comme `Error` — nouveau helper `src/lib/errors.ts` (`extractErrorMessage`) applique partout. `ensureClub()` verifie et cree desormais une equipe par defaut si le club existant n'en a aucune (auto-reparation). Tous les appels `ensureClub()` dans les vues sont maintenant dans un `try/catch` qui affiche l'erreur reelle. Migration `20260911160000_clubs_fixes.sql` ajoutee : fusionne d'eventuels clubs en double pour un meme owner_id, ajoute la contrainte d'unicite manquante `clubs_owner_id_key`, cree une equipe par defaut pour tout club qui n'en a aucune.
- 11/09/2026 (suite) : Toujours en echec apres le fix precedent, avec cette fois le vrai message ("new row violates row-level security policy for table teams"). Diagnostic demande a l'utilisateur (requetes read-only) : `select * from pg_policies where tablename='teams'` renvoie **0 ligne** — aucune policy RLS n'existe sur `teams` en prod, alors que la migration `20260911140000_clubs_teams.sql` (qui en cree 4) avait ete reportee comme executee avec succes. La table `clubs` elle-meme est correcte (1 ligne, bon `owner_id`), et une equipe "Équipe 1" existe deja (probablement creee via le SQL editor lui-meme, qui contourne RLS — pas via l'app). Migration `20260911170000_recreate_club_policies.sql` ecrite pour recreer les 12 policies, en supposant `club_members` deja existante.
- 11/09/2026 (suite) : Cette hypothese etait fausse — l'utilisateur a essaye d'appliquer 170000 et obtenu `relation "club_members" does not exist`. Diagnostic complet : `clubs` existe (1 ligne correcte), `teams` existe (avec des donnees, mais 0 policy), `club_members` **n'existe pas du tout**. Confirme que la migration `20260911140000_clubs_teams.sql` s'est arretee en cours de route bien plus tot que suppose (avant meme de creer `club_members`), malgre le rapport de succes de l'utilisateur a l'epoque — probablement parce que le SQL editor Supabase execute les instructions sans transaction globale, et qu'un echec silencieux ou partiel n'a pas ete remarque. Migration `20260911170000` supprimee (ne pouvait pas s'appliquer) et remplacee par `20260911180000_fix_club_members.sql`, autonome : cree `club_members` de A a Z si absente, recree les 12 policies (clubs/teams/club_members), backfill la ligne `OWNER` manquante pour les clubs existants, avec verification finale.
- 11/09/2026 (suite) : Migration `20260911180000_fix_club_members.sql` appliquee avec succes par l'utilisateur — verification finale confirmee : 4 policies pour chacune des 3 tables (clubs, teams, club_members). Creation d'une 2e equipe ("Équipe Réserve · Division 3") reussie dans l'app.
- 11/09/2026 (suite) : Deux nouveaux bugs trouves en poursuivant le test. (1) Assigner une equipe a un joueur echoue avec "Could not find the 'team_id' column of 'players' in the schema cache" — confirme que la migration 140000 s'est arretee encore plus tot que prevu, avant meme d'ajouter club_id/team_id sur players et matches. Migration `20260911190000_players_matches_team_columns.sql` ecrite : ajoute les colonnes manquantes, backfill club_id (pas team_id, "Sans équipe" reste un choix valide du coach), termine par un `notify pgrst, 'reload schema'` au cas ou le cache PostgREST serait en cause. (2) Tous les `<select>` de l'app (equipe, buteur/passeur, carton, remplacement) affichaient un menu deroulant illisible (fond blanc, texte invisible) faute de `[color-scheme:dark]` — present sur l'input date de CreateMatchModal mais oublie sur les select ajoutes depuis. Ajoute aux 8 select concernes (CreateMatchModal, RosterView x2, SubstitutionModal x2, CardPlayerModal, GoalDetailsModal x2). Build et typecheck valides.
- 11/09/2026 (suite) : Migration `20260911190000` appliquee, assignation d'equipe a un joueur confirmee fonctionnelle par l'utilisateur (Marco → Équipe Réserve). Le fix `[color-scheme:dark]` sur les select etait insuffisant : sur Chrome/Windows, les `<option>` du popup natif restaient rendues en clair (fond blanc, texte illisible), `[color-scheme:dark]` n'agissant pas de facon fiable sur le contenu du popup selon navigateur/OS. Fix definitif : regle CSS globale (`select option { background-color / color }`) dans `src/assets/main.css`, qui s'applique a tous les select de l'app sans avoir a toucher chaque composant. Utilisateur confirme que les menus deroulants sont enfin lisibles.
- PR #2 (profil joueur + fondation multi-club Phase A + tous les fix de migrations/UI ci-dessus) **mergee dans `main`**.
- 11/09/2026 (suite) : Demarrage de la Phase B (securite reelle par equipe), sur une nouvelle branche `claude/phase-b-team-rls`. Migration `20260911200000_phase_b_team_rls.sql` : deux fonctions SQL `security definer` (`can_access_team(club_id, team_id)`, `is_club_member(club_id)`) pour eviter de dupliquer la logique d'acces et toute recursion RLS ; suppression dynamique (via `pg_policies`, noms inconnus car ecrites avant ce projet) des anciennes policies `created_by`-based sur `players`/`matches`/`events`/`match_lineups`/`reports`, remplacees par des policies basees sur l'equipe. Players en lecture reste club-wide (voir Decisions Prises) pour ne pas casser l'affichage des "renforts" inter-equipes. Policies SELECT de `clubs`/`teams` elargies a tout membre actif (pas que le OWNER), en prevision de la Phase C. Build/typecheck valides (migration SQL uniquement, aucun changement frontend).
- 11/09/2026 (suite) : Migration Phase B appliquee par l'utilisateur, parcours complet reteste (effectif, match, rapport) sans regression pour le OWNER — confirme que le cloisonnement RLS par equipe ne casse rien pour l'usage actuel mono-coach. PR #3 mergee dans `main`.
- 11/09/2026 (suite) : A la demande de l'utilisateur, retour sur la branche `claude/compassionate-feynman-bgdief` (reinitialisee sur `main` a jour, incluant Phases A+B) pour ajouter une recherche/filtre de joueurs. `RosterView.vue` : barre de recherche par nom (insensible a la casse, sur `players.name`), filtre par poste (liste deduite dynamiquement des postes deja saisis dans l'effectif), combinable avec le filtre par equipe deja existant. Aucune migration necessaire (recherche cote client sur les donnees deja chargees). Build/typecheck valides.
- 11/09/2026 (suite) : A la demande de l'utilisateur, le formulaire d'ajout de joueur (masque par defaut depuis le fix precedent) devient une popup plutot qu'un bloc retractable inline, meme style que les autres modales de l'app. Puis meme demande pour l'equipe (TeamsView), accompagnee d'une question sur les infos utiles a ajouter aux equipes ("juste le nom, c'est pas tres utile"). Ajout de `category` (Seniors, U19...) et `formation` (ex: 4-4-2) sur `teams` (migration `20260911210000_teams_category_formation.sql`), plus un nombre de joueurs par equipe calcule a la volee depuis l'effectif (pas stocke, pour ne pas avoir a le maintenir a jour manuellement). Build/typecheck valides.
- 11/09/2026 (suite) : Migration `20260911210000_teams_category_formation.sql` appliquee et parcours reteste avec succes par l'utilisateur (popups joueur/equipe, categorie/formation, nombre de joueurs affiche). PR #4 mergee dans `main`.
- 11/09/2026 (suite) : Implementation de la Phase C (invitations par email), sur une nouvelle branche `claude/phase-c-invitations`. Migration `20260911220000_phase_c_invitations.sql` (fonction `accept_pending_invites()`), reecriture de `clubsStore.ensureClub()` (proprietaire → coach invite → creation en dernier recours), nouveau store `clubMembers.store.ts`, vue `/members`, restriction du choix d'equipe aux coachs non-proprietaires dans CreateMatchModal/RosterView, et masquage des actions de gestion d'equipe pour les non-proprietaires dans TeamsView. Build/typecheck valides. **Pas encore teste en conditions reelles (necessite un 2e compte de test), migration pas encore appliquee.**
- 11/09/2026 (suite) : Migration `20260911220000_phase_c_invitations.sql` appliquee par l'utilisateur, test reel demarre. Incident de manipulation (pas un bug) : l'utilisateur a cree un 2e compte Supabase Auth directement (`ngoyi.jeremie.pro@gmail.com`) au lieu de se faire inviter par le 1er compte, ce qui l'a rendu proprietaire de son propre club. Ne pouvant pas supprimer de compte moi-meme (aucun acces DB/admin dans cette session distante), j'ai guide l'utilisateur vers Supabase Dashboard → Authentication → Users pour le supprimer, en expliquant que le `on delete cascade` sur `clubs.owner_id` nettoierait les lignes liees, puis rappele la bonne sequence (inviter depuis le compte proprietaire, ensuite seulement se connecter avec le compte invite).
- 11/09/2026 (suite) : Apres suppression du compte et reprise du test, l'utilisateur a signale une limite du modele : l'invitation ne permet de rattacher un coach qu'a une seule equipe, alors que son scenario reel a un coach de l'equipe 2 qui est aussi responsable des equipes seniors et doit donc avoir acces a plusieurs equipes. Question posee sur la pertinence d'un systeme de roles. Discussion en 2 temps avec l'utilisateur (voir Decisions Prises) : il a lui-meme propose de prioriser et d'avancer pas a pas plutot que de tout livrer d'un coup, et a valide de resoudre immediatement le cas concret (multi-equipe) sans construire toute la hierarchie de roles (president/responsable de categorie) pour l'instant. Implementation : migration `20260911230000_multi_team_membership.sql` (table `club_member_teams`, backfill depuis l'ancienne colonne `club_members.team_id` puis suppression de cette colonne, RLS, `can_access_team()` reecrite). Cote client : `Membership.teamIds: string[]` dans `clubs.store.ts` (`ensureClub()` fait une requete supplementaire sur `club_member_teams` pour le membre courant), `clubMembers.store.ts` reecrit pour lire/ecrire les liaisons multi-equipes, `MembersView.vue` passe d'un `<select>` unique a une liste a cocher pour choisir les equipes a l'invitation et affiche toutes les equipes de chaque membre, `selectableTeams` (CreateMatchModal, RosterView) filtre sur `membership.teamIds.includes(...)`. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Migration pas encore appliquee, pas encore testee en conditions reelles.**
- 11/09/2026 (suite) : Test complet du parcours multi-equipe confirme fonctionnel de bout en bout par l'utilisateur. Trois retours supplementaires en poursuivant le test : (1) le nom "Historique" pour la liste des matchs preterait a confusion et aucun badge/filtre par equipe n'etait visible dans cette liste alors que `matches.team_id` existe en base depuis la Phase A — renomme "Matchs", badge equipe ajoute sur chaque ligne, filtre par equipe ajoute des qu'il y en a plus d'une (`HistoryView.vue`). (2) `TeamsView.vue` affichait un nombre de joueurs par equipe mais aucun moyen de voir qui en fait partie — rendu cliquable pour deplier la liste nominative (nom + poste). Aucune migration necessaire (donnees deja en base). Build/typecheck valides. (3) L'utilisateur a suggere de remplacer "Inviter un coach" par "Inviter un membre" avec un choix de role (joueur/coach/autre) dans la popup, tout en demandant explicitement si c'etait le bon moment vu la mise en pause recente de la hierarchie de roles — non implemente pour l'instant : voir echange en cours, la question reste ouverte a la prochaine reponse de l'utilisateur.
- 11/09/2026 (suite) : L'utilisateur a clarifie sa demande sur le role OWNER et l'invitation par role. Sur le OWNER : dans la vie reelle il representera le president du club qui ne fera QUE consulter les donnees, mais pendant que lui (le createur/admin de l'outil) construit et teste tout seul, il doit garder le controle total pour ne pas s'auto-bloquer — distinction explicite entre "phase de construction actuelle" et "usage reel futur" (voir Decisions Prises). Sur l'invitation par role : confirme que ce n'est pas encore pour un vrai joueur (on code encore), mais que le systeme cible est bien qu'un membre autorise choisisse le role de la personne invitee, joueur inclus, avec une vraie case dediee. Implementation en consequence : `club_members.role` accepte desormais 'PLAYER' et 'OTHER' en plus de 'OWNER'/'COACH' (migration `20260911240000_member_roles_and_read_only_access.sql`), nouvelle fonction SQL `can_write_team()` (OWNER + COACH uniquement) distincte de `can_access_team()` (lecture, tout role) — les policies d'ecriture de players/matches/events/match_lineups/reports basculent sur `can_write_team()`, la lecture reste inchangee. Cote client : `MemberRole` etendu, `clubsStore.canWrite`, popup d'invitation avec selecteur de role (Coach/Joueur/Autre), boutons d'ecriture (nouveau match, ajouter/modifier/archiver un joueur) masques pour les roles lecture seule dans `HistoryView.vue`/`RosterView.vue`. `TrackerView` pas encore gate cote UI pour ces roles (note dans Prochaines etapes), la RLS protege deja les donnees dans tous les cas. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Migration pas encore appliquee, pas encore testee en conditions reelles.**
- 11/09/2026 (suite) : Les 3 migrations en attente (Phase C, multi-equipe, roles PLAYER/OTHER) appliquees et testees avec succes en conditions reelles par l'utilisateur. Seule limite reelle trouvee au passage : impossible de rattacher une equipe a un match deja cree (CreateMatchModal ne gere que la creation) — juge non bloquant par l'utilisateur, contourne par une requete SQL manuelle plutot que par une feature d'edition. PR #5 (tout le travail Phase C + multi-equipe + roles + visibilite equipe/match) **mergee dans `main`**.
- 11/09/2026 (suite) : L'utilisateur a partage en une fois toute une liste de sujets accumules pendant qu'on travaillait : stats par equipe, question sur l'acces cross-equipe (coach equipe 1 voit-il les donnees de l'equipe 2 ?), etiquette "promotion" a afficher dans les stats d'un joueur appele en renfort (deja fait en fait — `totals.calledUpCount` existe et s'affiche dans `PlayerProfileView.vue`, juste pas remarque), securite contre la creation de faux clubs (pas de B2C ouvert voulu), question sur la reconnexion systematique a chaque rechargement de page, hierarchie de roles complete avec pages profil (admin/president/coach/adjoint/dirigeant/joueur, qui peut voir/modifier/supprimer quoi), refonte de la home en bento design, et renommage de l'app en "NRV". Fait immediatement (rapide, sans ambiguite) : renommage "NRV" dans le header (`HistoryView.vue` uniquement pour l'instant — pas encore etendu a `AuthView.vue`/`index.html`/manifest). Le reste attend une reponse/priorisation de l'utilisateur avant d'etre code (liste complete dans Prochaines etapes) — a l'exception de la securite des faux clubs, discutee ensuite.
- 11/09/2026 (suite) : Sur la securite des faux clubs, l'utilisateur hesitait sur l'approche (doublons de noms de club, "je sais pas comment faire la difference"). Propose et implemente : validation manuelle par l'admin plutot que verification de nom (voir Decisions Prises — un nom unique ne prouve rien). Migration `20260911250000_club_approval_status.sql` : colonne `clubs.status` ('PENDING' par defaut pour un club cree au premier login sans invitation, 'ACTIVE' pour les clubs deja en place), `is_club_member()`/`can_access_team()`/`can_write_team()` exigent desormais un club `ACTIVE` en plus de l'appartenance — bloque tout usage fonctionnel d'un club en attente, y compris pour son proprietaire. Nouvelle vue `/pending` (`PendingApprovalView.vue`) et guard de routeur qui redirige vers elle si le club n'est pas encore valide. Validation par l'admin : requete SQL manuelle pour l'instant (fournie en commentaire dans la migration), pas de page admin dediee. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Migration pas encore appliquee, pas encore testee en conditions reelles.**
- 11/09/2026 (suite) : Investigation du bug de reconnexion a chaque F5, signale par l'utilisateur dans son gros message groupe. Diagnostic en 2 temps : (1) relecture du code d'auth (client Supabase, store, garde de routeur) ne montrait rien d'anormal, persistance activee par defaut ; question posee sur le contexte de test (URL stable vs lien de preview qui change vs navigation privee) pour ecarter les causes environnementales — reponse : meme URL, simple F5. (2) Verification DevTools demandee : la cle `sb-*-auth-token` restait bien presente en localStorage apres rechargement, ce qui pointait vers un vrai bug applicatif plutot qu'un probleme navigateur. Cause trouvee : `src/main.ts` retardait `app.mount()` jusqu'a la restauration de la session (`authStore.init()`), mais pas `app.use(router)` — Vue Router declenche sa navigation initiale (et donc la garde d'authentification) des l'appel de `app.use(router)`, independamment du montage. La garde s'executait donc avant que `authStore.user` soit restaure depuis la session persistee, le trouvait a `null`, et redirigeait vers `/auth` a chaque fois. Fix : `app.use(router)` deplace apres `await authStore.init()`, dans le meme bloc async que le montage. Build/typecheck valides.
- 11/09/2026 (suite) : Migration `20260911250000_club_approval_status.sql` appliquee par l'utilisateur, ouverture de la PR #6 pour deployer une preview testable (branche `claude/nrv-rename-and-club-safety`, pas encore de deploiement pour une branche sans PR). Premier test de la validation de club : nouveau compte cree, le club apparait bien en base avec `status: 'PENDING'`, mais l'utilisateur n'atterrit PAS sur `/pending` — accede directement a `/history`. Root cause trouvee : le compte de test precedent (avec un club deja `ACTIVE`) avait ete deconnecte dans le MEME onglet juste avant, sans rechargement complet de page. Aucun store Pinia (`clubsStore` en tete) n'etait jamais reinitialise a la deconnexion ou au changement de compte — `clubsStore.club` gardait donc en memoire le club `ACTIVE` du compte precedent. La garde de routeur, voyant `clubsStore.club` deja rempli, ne rappelait pas `ensureClub()` pour le nouveau compte et verifiait le statut sur ce club perime (`ACTIVE`) au lieu du vrai club `PENDING` du nouveau compte. Bug plus large que le seul ecran `/pending` : n'importe quelle donnee (equipes, joueurs, matchs...) d'un compte precedent pouvait rester visible en memoire apres un changement de compte dans le meme onglet, avant qu'une vue ne la re-fetch. Fix dans `auth.store.ts` : un `currentUserId` suit l'identite courante, et des que `onAuthStateChange` rapporte un changement d'identite (deconnexion, connexion sous un autre compte), tous les stores contenant des donnees liees a un compte/club (`clubs`, `clubMembers`, `teams`, `players`, `playerProfile`, `match`, `events`, `lineup`, `report`) sont vides via leurs `reset()` respectifs (ajout direct pour `match.store.ts`, seul sans `reset()` existant). Build/typecheck valides.
- 11/09/2026 (suite) : Reconnexion + validation de club reconfirmes fonctionnels par l'utilisateur apres le fix precedent. Deux nouvelles demandes fortes dans la foulee (une exprimee avec insistance, a raison) : (1) le nom du club n'est jamais demande a la creation ("on vient de creer un compte et apparemment le club est deja cree c'est n'importe quoi !!!!!!"), corrige en supprimant toute creation automatique de club — `ensureClub()` leve desormais `ClubNotFoundError` et la garde de routeur redirige vers une nouvelle vue `/create-club` ou l'utilisateur choisit le nom avant creation (`clubsStore.createClub(name)`). (2) demande explicite d'une page admin avec un tableau et des boutons pour valider les clubs en attente, au lieu de la requete SQL manuelle donnee jusque-la — nouvelle vue `/admin/clubs` (`AdminClubsView.vue`) + store `adminClubs.store.ts`, avec un bouton "Admin" visible uniquement pour les admins dans le header. En construisant cette page, trouve et corrige un vrai trou de securite (voir Decisions Prises) : n'importe quel proprietaire de club pouvait passer lui-meme son statut a `ACTIVE` via l'API, rendant la validation manuelle inutile en pratique — ferme par une table `platform_admins` (seedee avec le compte de l'utilisateur via son email) et un trigger `BEFORE UPDATE` qui bloque tout changement de `clubs.status` hors admin, en plus des nouvelles policies select/update/delete necessaires pour qu'un admin gere n'importe quel club. Migration `20260911260000_platform_admins_and_club_approval.sql`. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides.
- 11/09/2026 (suite) : Migration `20260911260000_platform_admins_and_club_approval.sql` appliquee. Premier test de `/create-club` fait avec un compte (`test@gmail...`) qui possedait deja un club `ACTIVE` d'un test precedent — redirection vers `/history` au lieu du formulaire, comportement attendu (un compte avec un club existant n'a pas besoin d'en creer un) mais explique a l'utilisateur pour lever le doute. **Reteste avec un compte reellement neuf par l'utilisateur : confirme fonctionnel de bout en bout** (`/create-club` -> nom choisi -> `/pending` -> validation via `/admin/clubs`). PR #6 mergee dans `main`.
- 11/09/2026 (suite) : Demande d'une page "Mon club" regroupant equipes et effectif. Question posee pour clarifier l'architecture (fusion complete remplaçant /teams et /roster, ou page d'apercu qui renvoie vers elles) — reponse : fusion complete. Nouvelle branche `claude/club-page` (repartie de `main` a jour, PR #6 deja mergee). Extraction du contenu de `TeamsView.vue`/`RosterView.vue` vers 2 composants (`src/components/club/TeamsSection.vue`, `RosterSection.vue`, sans leur propre header de page), montes dans une nouvelle vue `ClubView.vue` a onglets (Effectif / Equipes, persistes dans l'URL via `?tab=`). Toutes les navigations internes mises a jour, anciennes routes `/teams`/`/roster` (liste) gardees en redirections vers `/club?tab=...` pour la compatibilite. `RosterView.vue`/`TeamsView.vue` supprimes. Header `HistoryView.vue` : "Equipes"/"Effectif" fusionnes en un seul bouton "Mon club". Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore teste en conditions reelles par l'utilisateur.**
- 11/09/2026 (suite) : PR #7 mergee apres 2 retours rapides de l'utilisateur (nom reel du club affiche au lieu de "Mon club"/"Membres du club" generiques). L'utilisateur demande ensuite d'arreter le workflow branche+PR et de travailler directement sur `main`, "comme si c'etait [s]on compte qui creait les branches" — clarifie que les commits restent attribues a Claude (regle fixe de la session, pas negociable) mais que le repo est deja le sien de toute facon. Question posee pour trancher le vrai point en jeu (push direct sur `main`, avec deploiement prod immediat, vs branche + merge par Claude sans passer par l'ecran GitHub) — reponse : direct sur `main`. Local repasse sur `main` a jour (PR #6 et #7 mergees). Puis, l'utilisateur laisse la priorisation du reste du backlog a l'appreciation de Claude ("je sais pas fais dans l'ordre qui te semble juste"). Ordre choisi et pousse directement sur `main` en un seul lot : (1) nettoyage d'une ligne dupliquee dans ce fichier, (2) renommage "NRV" etendu a `AuthView.vue`/`index.html`/`public/manifest.webmanifest` (jusque-la fait uniquement dans le header), (3) `TrackerView.vue` gate cote UI pour les roles lecture seule (nouveaux props `canControl` sur `MatchTimer.vue` et `readOnly` sur `EventLog.vue`, `ActionButtons`/bouton "Terminer le match" masques, bandeau "Lecture seule", passage auto en `LIVE` au montage plus tente pour un lecteur seul). Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore teste en conditions reelles.** Prochain chantier : stats par equipe.
- 11/09/2026 (suite) : Implementation des stats par equipe dans `TeamsSection.vue` (matchs joues, V/N/D, buts pour/contre, sur les matchs `FINISHED`). Point technique cle identifie en lisant `TrackerView.vue` : `scoreHome`/`scoreAway` sont deja, par construction du tracker (GOAL_FOR/GOAL_AGAINST), "nos buts"/"buts encaisses" et non des scores domicile/exterieur au sens propre — les champs `homeTeam`/`awayTeam` ne sont que des libelles texte libre saisis a la creation du match. Ca simplifiait le calcul victoire/nul/defaite sans ambiguite. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides, pousse directement sur `main`. **Pas encore teste en conditions reelles.**
- 11/09/2026 (suite) : A la demande de l'utilisateur de continuer la liste, gate `LineupView.vue` (seule page de saisie encore non protegee pour les roles lecture seule) sur le meme principe que `TrackerView.vue` : `cycleRole()` ne fait plus rien si `!clubsStore.canWrite`, actions d'ecriture masquees (bouton "Creer l'effectif", "Passer"/"Demarrer le match"), remplacees par un acces direct en lecture au match. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides, pousse directement sur `main`. Prochain chantier : page d'accueil "bento design".
- 11/09/2026 (suite) : Implementation de la page d'accueil "bento design". `/history` (l'ancienne home, liste "Matchs" avec le gros header de nav) redevient une sous-page classique, et une nouvelle vue `HomeView.vue` (route `home`, `/` redirige vers `/home`) devient la vraie page d'accueil : grille de cartes (prochains matchs, effectif, equipes avec initiale du club en guise de fanion, derniers resultats). Toutes les redirections post-action et boutons retour de l'app repointes vers `home` au lieu de `history`. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides, pousse directement sur `main`. **Pas encore teste en conditions reelles.**
- 11/09/2026 (suite) : A la demande de l'utilisateur ("Fais la revue RLS"), revue de securite complete du projet Supabase reel — **premiere fois dans cette session avec un acces direct au projet via MCP** (`get_advisors`, `execute_sql`, `apply_migration`), au lieu de devoir tout faire relire/executer par l'utilisateur en SQL Editor. Verifie que les definitions de policies/fonctions en base correspondent exactement aux migrations (aucune regression comme lors des incidents de partial-apply du debut de session). Trou trouve : les fonctions `security definer` (`accept_pending_invites`, `can_access_team`, `can_write_team`, `is_club_member`, plus le trigger `enforce_club_status_change` et l'event trigger Supabase `rls_auto_enable`) etaient executables par le role `anon` non-authentifie via RPC — pas exploitable en pratique mais mauvaise hygiene. Fix applique directement en prod : `revoke execute ... from anon` sur les 6, `authenticated` conserve. Verification faite en 2 temps : un premier essai de revoke sur `PUBLIC` n'a rien change (Supabase accorde separement `EXECUTE` a `anon`/`authenticated` par defaut, independamment du grant PUBLIC) — corrige en revoquant explicitement `anon`. Par prudence, j'ai aussi releve puis immediatement re-accorde `EXECUTE` a `authenticated` sur les 2 fonctions trigger (le temps de m'assurer de ne pas casser le trigger d'approbation de club sans pouvoir retester immediatement en conditions reelles avec un vrai compte connecte). Confirme par lecture directe de `information_schema.routine_privileges` (avant/apres) plutot que de se fier seul a `get_advisors`, qui a mis quelques secondes a se rafraichir. Migration `20260911270000_lock_down_function_grants.sql`. Deux points restent hors de portee d'un fix automatique : "Leaked Password Protection" desactive (reglage bascule dans Auth Dashboard, pas en SQL) et une question produit trouvee au passage (voir Decisions Prises / Prochaines etapes) sur la visibilite du nom d'une equipe pour un coach non-admin qui n'y est pas rattache.
- 11/09/2026 (suite) : L'utilisateur confirme vouloir la lecture club-wide des equipes, comme pour les joueurs. Policy `teams_select_member` reecrite : `is_club_member(club_id)` (club-wide) au lieu de `can_access_team(club_id, id)` (limite aux equipes rattachees). L'ecriture (creer/modifier/supprimer une equipe, toujours reservee au OWNER) n'est pas touchee. Aucun changement cote client necessaire (le filtrage `selectableTeams` pour l'assignation/creation reste base sur `membership.teamIds`, independant de la lecture). Migration `20260911280000_teams_club_wide_read.sql`, **appliquee directement en prod par Claude et verifiee** (lecture de `pg_policies`).
- 12/09/2026 : A la demande de l'utilisateur ("On avance sur la hierarchie de roles complete"), reprise du chantier explicitement mis en pause a plusieurs reprises. Avant de coder, 3 questions posees pour cadrer un changement de schema/policies pousse directement en prod sans PR : scope du President (tout le club en lecture, confirme), mecanisme du Responsable de categorie (par categorie, automatique, confirme), et modele Adjoint/Dirigeant (reponse detaillee mais avec une ambiguite reelle — voir Decisions Prises pour l'arbitrage tranche sur l'ecriture de l'Adjoint). Lecture prealable des fonctions `can_access_team`/`can_write_team`/`is_club_member` et des policies d'ecriture de `players`/`matches`/`events`/`match_lineups`/`reports` en base (toutes basees sur `can_write_team`, aucune distinction "gestion club" vs "operations match" a l'interieur de cette fonction — confirme que le scope naturel d'ADJOINT/CATEGORY_MANAGER dans `can_write_team()` couvre exactement effectif+matchs+tracker+rapports, jamais la creation d'equipe ni les invitations qui passent par des policies separees testant `OWNER` explicitement).
- 12/09/2026 (suite) : Implementation. Migration `20260912090000_role_hierarchy_president_category_adjoint.sql` : elargissement du CHECK sur `club_members.role` (+PRESIDENT/CATEGORY_MANAGER/ADJOINT, DIRIGEANT explicitement absent), nouvelle table `club_member_categories` (many-to-many, RLS calquee sur `club_member_teams` — select si membre ou OWNER du club, insert/delete OWNER uniquement), et reecriture de `can_access_team()`/`can_write_team()` pour integrer les 3 nouveaux roles (PRESIDENT bypass lecture club-wide, CATEGORY_MANAGER via jointure `club_member_categories`/`teams.category`, ADJOINT ajoute a la meme branche que COACH dans `can_write_team()`). **Appliquee directement en prod par Claude**, puis verifiee par une simulation complete en transaction annulee (`BEGIN ... ROLLBACK`, jamais commitee) : un membre reel existant temporairement bascule successivement en PRESIDENT/CATEGORY_MANAGER/ADJOINT (role + rattachements modifies puis restaures par le rollback), `SET LOCAL request.jwt.claim.sub` pour simuler son JWT, resultats de `can_access_team`/`can_write_team` captures dans une table temporaire (`RAISE NOTICE` n'etant pas visible via l'outil SQL) : les 3 roles se comportent exactement comme specifie, y compris la decouverte rassurante que CATEGORY_MANAGER accede bien a une 2e equipe partageant la meme categorie (preuve que l'automatisme "toute equipe de cette categorie, actuelle ou future" fonctionne). `get_advisors`(security) reverifie : aucune nouvelle regression, les 2 findings deja connus (acces `authenticated` volontaire, Leaked Password Protection Pro-only) inchanges.
- 12/09/2026 (suite) : Cote client, extension de `MemberRole`/`Membership` (`clubs.store.ts`, nouveau champ `categories: string[]` peuple par `ensureClub()` via une requete sur `club_member_categories`), `canWrite` etendu a ADJOINT/CATEGORY_MANAGER, nouveau helper `hasTeamAccess(team)` (rattachement direct OU categorie partagee) qui remplace les filtres `selectableTeams` bases uniquement sur `teamIds` dans `CreateMatchModal.vue` et `RosterSection.vue`. `clubMembers.store.ts` : `ClubMember.categories`, `fetchMembers()` recupere aussi les liaisons de categories, `inviteMember()` accepte desormais soit des `teamIds` soit des `categories` selon le role (validation conditionnelle : PRESIDENT n'a besoin d'aucun des deux). `MembersView.vue` : 2 nouvelles options dans le selecteur de role (grille passee de 3 a 2 colonnes vu qu'il y a maintenant 6 roles invitables), formulaire adaptatif (`needsTeams`/`needsCategories` computed) qui affiche soit la liste d'equipes a cocher, soit une liste de categories deduites des equipes existantes, soit rien du tout pour PRESIDENT, et un `accessSummary()` qui affiche le bon resume par role dans la liste des membres. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore teste en conditions reelles** (necessite d'inviter un vrai compte sous ces roles) — prochaine etape avant de considerer le chantier termine.
- 12/09/2026 (suite) : Demande de l'utilisateur suite au test de la popup d'invitation : pour un JOUEUR ou un membre "Autre", cliquer une equipe doit desormais griser les autres (impossible d'en cocher une 2e sans decocher la premiere), alors que COACH/ADJOINT/CATEGORY_MANAGER restent multi-equipes. Implemente dans `MembersView.vue` : nouveau computed `singleTeamRole`, `toggleTeam()` remplace le tableau au lieu d'y ajouter quand ce role est actif, cases a cocher non selectionnees grisees/desactivees tant qu'une equipe est cochee, et bascule automatique vers une selection unique si l'utilisateur change de role apres avoir deja coche plusieurs equipes. Build/typecheck valides.
- 14/09/2026 : L'utilisateur signale que le style de l'app "fait trop prototype" et demande ce qu'il faut lui fournir pour une vraie refonte. Reponse donnee : liste des routes, code des vues principales (Tracker/Report/History/Home/Auth deja partages dans la conversation), distinction de roles existante, etat de l'ecran d'auth, et une auto-evaluation honnete de ce qui saute aux yeux comme "prototype" (palette repetitive, emojis en guise d'icones, aucune identite de marque, pas de page reglages club malgre `renameClub()` existant mais jamais appelee). Deux chantiers identifies en meme temps (restriction creation de match a Coach/Admin + designation par match, page reglages club) mis en attente a la demande de l'utilisateur ("Fais apres la refonte").
- 14/09/2026 (suite) : L'utilisateur fournit un zip complet ("refonte.zip") produit par une autre session Claude dediee au design : README de handoff, `01-tokens.md` (couleurs/typo/rayons/badges de role/icones, valeurs normatives), `02-screens.md` (spec des 7 ecrans avec mapping vers les fichiers du repo), `03-logo.md` + 4 SVG du logo NRV, et 7 maquettes `.dc.html` (HTML/CSS inline, valeurs a lire dans le markup). Contenu verifie en detail avant de commencer (tous les fichiers lus integralement). Confirmation donnee a l'utilisateur qu'aucune information supplementaire n'etait necessaire, puis demarrage immediat dans l'ordre suggere par le README : fondations (tokens+logo) -> Auth -> Home -> History -> Tracker -> Report -> Club -> Lineup.
- 14/09/2026 (suite) : Fondations posees (tokens Tailwind dans `main.css`, `NrvLogo.vue`, 4 SVG nettoyes des metadonnees C2PA volumineuses avant copie dans le repo, favicon/icones/manifest regeneres). AuthView refaite et **verifiee par une vraie capture d'ecran** (serveur de dev + Playwright headless, seule methode de verification visuelle disponible sans compte utilisateur reel) : a revele un vrai bug avant meme le premier commit valide (bouton CTA blanc au lieu de vert, cause = collision entre le nouveau token `--color-accent` et le token shadcn existant du meme nom, qui gagnait car declare plus bas dans le fichier) — corrige en renommant tous les tokens de marque en `brand-*`, reverifie par une 2e capture montrant le bouton vert. Poussee sur `main`.
- 14/09/2026 (suite) : HomeView reconstruite (bento). Nouveaux modules partages `roleBadge.ts`/`RoleBadge.vue` (badges des 7 roles reels), `matchBadges.ts` (statuts de match, V/N/D), `displayName.ts` (nom/initiales derives de l'email, faute d'un champ "nom" dans le modele). Trois adaptations documentees dans le commit (pas de ligne "fonde en 1924" fabriquee, pas de selecteur "3 pistes de blason" qui etait une exploration de style pour le client, menu avatar ajoute pour Membres/Admin/Deconnexion absents du mock). Impossible de verifier visuellement avec de vraies donnees (pas de compte de test dans cet environnement) — explique clairement a l'utilisateur.
- 14/09/2026 (suite) : L'utilisateur reagit en 4 points dans le meme message, en plein milieu du travail sur History : (1) demande d'ajouter la ligne "fonde en 1924" comme vraie donnee editable via une page de reglages du club ; (2) confirme "pas de selecteur de piste de blason" ; (3) confirme le menu avatar ; (4) reprend vivement la formulation "je ne peux pas me connecter a ta vraie base Supabase", a raison — clarification apportee : l'acces direct a la base Supabase (MCP, utilise depuis le debut de la session pour toutes les migrations) est bien disponible et distinct de l'absence de compte utilisateur pour previsualiser l'app dans un navigateur authentifie. Implementation immediate : migration `clubs.founded_year`/`clubs.location` (nullables, appliquee en prod), nouvelle vue `/club/settings` (proprietaire uniquement), lien depuis le menu avatar de HomeView, `updateClubInfo()` remplace `renameClub()`. HomeView mise a jour pour afficher la vraie ligne meta.
- 14/09/2026 (suite) : HistoryView refaite (liste dense, tabs d'equipe, bilan V/N/D). Puis TrackerView (ecran critique, le plus gros morceau) : restructuration en 4 zones fixes + une seule zone scrollable (changement structurel reel, pas juste visuel), terrain passe en portrait avec rotation a 90° de la logique de zone dans `PitchMap.vue`, palette reduite a 11 evenements (ecart non documente comme "assume" dans le handoff mais normatif dans 2 documents source — signale explicitement a l'utilisateur pour validation), 3 modales de confirmation reskinees au passage (emojis retires, icone Lucide `X`). Nouveau module `eventPalette.ts`.
- 14/09/2026 (suite) : ReportView refaite (score compact au scroll, sections numerotees repliables) — adaptee a 5 sections reelles au lieu des 6 de la maquette, le contenu genere par l'edge function `generate-report` n'ayant que 4 champs + une liste d'ameliorations, sans niveau de confiance ; pas de contenu invente pour combler l'ecart. "Exporter en PDF" implemente via `window.print()` plutot que d'ajouter une dependance.
- 14/09/2026 (suite) : ClubView refaite (onglets soulignes, grilles de cartes) avec un **ecart majeur signale explicitement a l'utilisateur avant de continuer** : le handoff demande que l'onglet Effectif affiche les membres du club (`club_members`, roles) plutot que le roster de joueurs, mais aucun lien n'existe en base entre les deux tables — suivre le mock aurait exige d'inventer des numeros de maillot/postes pour des comptes utilisateur. Choix fait de garder l'onglet Effectif sur `players` (roster reel, reskinne) et de laisser `/members` distinct, avec 2 options proposees a l'utilisateur s'il veut vraiment le comportement du mock (lien reel en base, ou deux listes distinctes dans le meme onglet).
- 14/09/2026 (suite) : LineupView construite de zero (aucun equivalent avant cette refonte) — terrain de composition tap-to-place avec 3 formations, placement joueur<->position dans les deux sens, echange de titulaires. A necessite une vraie extension de schema (`matches.formation`, `match_lineups.slot_id`/`slot_label`, nullables et retrocompatibles) appliquee directement en prod, sans quoi la composition n'aurait pas pu etre persistee fidelement. Changement de comportement assume : plus de 3e etat "joueur non convoque", tout joueur actif de l'equipe du match est soit sur le terrain soit sur le banc (a documenter aupres de l'utilisateur si ca le surprend a l'usage). **Les 7 ecrans de la refonte NRV sont desormais livres.** `get_advisors`(security) reverifie a la fin : aucune regression sur les 2 findings deja connus. Prochaine etape : test en conditions reelles par l'utilisateur, et validation des 2 ecarts majeurs signales (palette Tracker, ClubView Effectif).
- 14/09/2026 (suite) : L'utilisateur tranche les 2 points laisses en suspens a la fin de la refonte. (1) "Corner concede"/"Coup franc concede" confirmes comme un oubli, pas un choix voulu — remis dans `eventPalette.ts` (13/13 types EventType de nouveau saisissables depuis le Tracker), couleurs reprises des anciens marqueurs terrain (orange/rose) faute de valeurs normatives dans le handoff pour ces 2 types. (2) Demande d'un vrai lien en base entre `players` et `club_members` (`players.member_id`, nullable, unique, migration appliquee en prod) pour que l'onglet "Membres" de ClubView puisse enfin exister comme prevu dans le handoff (grille de cartes avec badge de role, numero/poste affiches quand un membre est lie a une fiche joueur). Implementation : nouveau composant `MembersSection.vue` (reprend la logique d'invitation de `MembersView.vue`, desormais supprimee), `ClubView.vue` passe a 3 onglets (Effectif=roster players inchange / Membres=nouveau / Equipes inchange) plutot que de fusionner Effectif et Membres dans un seul onglet — compromis retenu pour ne perdre ni la gestion du roster (CRUD numeros/postes necessaire aux compositions) ni la vraie vue membres demandee par le handoff. `RosterSection.vue` gagne un champ optionnel "Compte lie" dans le formulaire joueur. Route `/members` redirige desormais vers `/club?tab=members` (meme pattern que `/teams`/`/roster`). Build/typecheck valides, migration verifiee en base, `get_advisors`(security) sans nouvelle regression.
- 14/09/2026 (suite) : Apres validation du 3-onglets ClubView ("Non c'est bon comme ca, si en utilisant je veux que ca change je te le dirai"), l'utilisateur a fait son **premier vrai test de la refonte dans un navigateur** et revient avec une longue liste de bugs/demandes accompagnee de 2 captures d'ecran (TrackerView mi-match avec un trait fait main marquant ou le scroll se bloque, et ReportView complet). Traite dans l'ordre suivant, tout pousse directement sur `main` en plusieurs commits, aucun ne pouvant etre revérifié visuellement dans un vrai navigateur authentifie depuis cette session :
  - **Bug ReportView** (`/match/.../report` sans rien affiche malgre des evenements reels en base, verifie par requete SQL directe sur le match cite) : la Chronologie et le pied de page etaient imbriques dans le `template v-else` qui ne s'affiche que si un rapport IA a deja ete genere — un match avec evenements mais sans rapport genere n'affichait donc litteralement rien. Chronologie sortie de ce bloc, rendue independante du rapport IA comme documente a l'origine (11/09/2026). Au passage : fond alterne par ligne + icone Lucide par type d'evenement (demande explicite, "les icones de foot n'etaient pas forcement mauvaises").
  - **Bug bouton "Enregistrer" de `/club/settings`** : meme classe de bug que celui deja rencontre sur RosterView (11/09/2026) — `foundedYear.value.trim()` appele hors try/catch sur une valeur qui peut etre un `number` selon le navigateur avec `<input type="number">`, `.trim()` levait silencieusement sans passer par le catch. Fix : `String(foundedYear.value).trim()`.
  - **Logo de club** : colonne `clubs.logo_url`, bucket de stockage Supabase public `club-logos` (policies : lecture publique, ecriture reservee au proprietaire du club sur son propre dossier `<club_id>/...`), `clubsStore.uploadLogo()`, champ URL + zone drag-and-drop dans `/club/settings`. Nouveau composant `ClubCrest.vue` (image ou fallback initiales) reutilise dans HomeView, ClubView (header) et PlayerProfileView (header). Migration `20260914130000_club_logo.sql`, **appliquee directement en prod par Claude**.
  - **`PlayerProfileView.vue` reconstruite avec le design NRV** (signalee par l'utilisateur comme non touchee par la refonte, confirme a la lecture — toujours en `bg-neutral-950`/emojis) : memes tokens que le reste de l'app, header avec navigation retour + crest du club.
  - **Navigation manquante dans `/club`** (signalee comme un vrai blocage, "impossible de revenir en arriere") : bouton retour + crest ajoutes dans le header de `ClubView.vue`.
  - **Onglet Membres** : filtre "Joueurs" retire (role PLAYER cote `club_members` n'a presque jamais de fiche `players` liee, ce filtre ne remontait donc structurellement rien — l'utilisateur a lui-meme identifie le probleme : "les joueurs sont dans effectif"), filtre "En attente" desormais reserve au proprietaire du club (`FILTERS` devient un computed conditionnel).
  - **Joueurs cliquables dans le detail d'equipe** (`TeamsSection.vue`, onglet Equipes de ClubView) — renvoient vers `/roster/:id`.
  - **Lineup : popup "reprendre le 11 du match precedent"** — au chargement d'un match sans composition, cherche le match anterieur le plus recent de la meme equipe avec une composition titulaire placee (slot_id renseigne), propose de la reprendre (formation + placement, filtre les joueurs qui ne seraient plus actifs).
  - **Temps additionnel + confirmation reset dans le Tracker** : `useTimer.ts` fige l'affichage a 45:00 par mi-temps et demarre un compteur separe "+X'" au-dela (la minute reelle des evenements continue de compter normalement). Le bouton "Terminer le match" existait deja avec sa propre confirmation (11/09/2026) — le vrai probleme etait qu'il etait inatteignable (bug de scroll ci-dessous). Bouton Reset du chrono : ajout d'une popup de confirmation (verifie que ca n'affecte pas les evenements deja saisis, contrairement a la crainte de l'utilisateur).
  - **Bug de scroll mobile dans TrackerView/ReportView/LineupView** (capture d'ecran a l'appui) : les 3 ecrans a "layout fige" (une seule zone scrollable) utilisaient `h-screen` (100vh, fixe) au lieu de `h-dvh` (viewport visible reel) — sur mobile, quand la barre d'adresse est affichee, 100vh reste plus grand que la zone reellement visible, ce qui rendait le bas de la timeline (dont le bouton "Terminer le match") inatteignable au scroll. Fix simple par remplacement de classe.
  - **Nouveaux types d'evenement "Faute commise"/"Faute subie"** (`FOUL_COMMITTED`/`FOUL_SUFFERED`) ajoutes a la palette Tracker (icone Lucide `Hand`) et aux libelles de la Chronologie — aucune migration necessaire (`events.type` est un simple `text` sans contrainte CHECK en base, verifie avant d'ajouter).
  - **Nouvelle page stats par equipe** (`/club/teams/:id/stats`, `TeamStatsView.vue`) : bilan matchs/buts pour-contre/V-N-D, split victoires domicile/exterieur (**approxime** en comparant `homeTeam` au nom reel de l'equipe du club, faute d'un champ booleen dedie dans `matches` — signale en Prochaines etapes), cartons et fautes agreges depuis les evenements des matchs termines, historique des 10 derniers matchs. Accessible depuis la carte Equipes de la Home et depuis le detail d'equipe de ClubView.
  - **`AdminClubsView.vue` reconstruite avec le design NRV** (meme constat que PlayerProfileView, restee en `neutral-950`).
  - `get_advisors`(security) reverifie apres l'ajout des policies du nouveau bucket de stockage : toujours les 2 seuls findings deja connus, aucune regression.
- 15/09/2026 : L'utilisateur revient sur cette premiere vague avec un nouveau point : le header commun (logo + menu avatar) n'etait toujours pas present sur toutes les pages malgre la nav retour ajoutee la veille sur `/club` — il a colle le markup exact de l'en-tete de HomeView pour demander qu'il soit repris partout. Extraction en un composant partage `AppHeader.vue` (logo + menu Membres/Reglages du club/Admin/Deconnexion, prop `back` optionnelle pour une fleche retour vers `/home`), monte sur toutes les pages de l'espace connecte qui n'avaient qu'un bouton retour ou rien du tout (`ClubView`, `ClubSettingsView`, `HistoryView`, `PlayerProfileView`, `AdminClubsView`, `TeamStatsView`). Meme message : la popup "Nouveau match" (`CreateMatchModal.vue`) restait dans l'ancien style (`bg-neutral-900`/blanc) malgre la refonte des autres popups — corrigee, et audit du reste du repo (`grep bg-neutral-900/bg-neutral-950`) qui a aussi remonte `PendingApprovalView.vue` et `CreateClubView.vue`, reskinees au passage.
- 15/09/2026 (suite) : En poussant ce lot, conflit avec un commit fait directement par l'utilisateur sur `main` pendant la session ("Fix layout of the chrono section in MatchTimer component") qui contenait des marqueurs de conflit Git non resolus (`<<<<<<<`/`=======`/`>>>>>>>`) laisses dans le fichier — aurait casse le build s'il n'avait pas ete corrige. Merge fait, marqueurs retires en gardant l'intention du commit (chrono en colonne).
- 15/09/2026 (suite) : Reprise du reste du backlog, point par point avec l'utilisateur. Deux questions posees avant de coder (voir Decisions Prises) : le mode de validation des donnees saisies par un joueur designe "live tracker", et la collision entre le libelle "President" voulu pour OWNER et le vrai role PRESIDENT deja existant. Reponses : ecriture directe sans file de validation separee ; ne rien changer au libelle OWNER (l'utilisateur n'avait pas remarque la collision). Implemente ensuite dans la foulee : delegation de tracking par match (nouveau champ `matches.designated_tracker_member_id`, fonction `is_match_tracker()`, policies RLS matches/events/match_lineups etendues, select dans `CreateMatchModal.vue`, `canTrack` dans `TrackerView.vue`) ; role DIRIGEANT (lecture club-wide comme PRESIDENT, jamais d'ecriture, ne peut pas inviter — reutilise le meme bypass que PRESIDENT dans `can_access_team()`). "Creation de match + lancement du tracker restreints a Coach/Admin" : deja le cas via `clubsStore.canWrite`, confirme sans changement necessaire (la nouveaute est la delegation ponctuelle, pas un assouplissement de cette regle). Rotation des secrets : confirmee faite par l'utilisateur, retiree du backlog. "Leaked Password Protection" : abandonne definitivement (necessite Supabase Pro, l'utilisateur ne veut pas payer) — retire du backlog, ne plus le remonter. Monitoring d'erreurs (Sentry) et le futur flux d'inscription en libre-service des joueurs : discussion/opinion donnees en conversation (pas encore code, l'utilisateur reflechit encore a l'authentification pour ce dernier point), voir Decisions Prises pour le raisonnement. Pages profil + permissions de suppression : ecartees par l'utilisateur, le vrai besoin (retirer un joueur du club) passera par l'inscription en libre-service a venir plutot que par une gestion manuelle des droits. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides a chaque etape, `get_advisors`(security) reverifie apres les migrations : toujours les memes findings deja connus (acces `authenticated` volontaire, Leaked Password Protection desormais assume comme non traite). **Rien de tout cela teste en conditions reelles.**
- 15/09/2026 (suite) : Longue session de mise au point du test e2e (`npm run e2e`) avec l'utilisateur, qui n'avait jamais tourne jusqu'au bout depuis la refonte NRV. Corriges dans l'ordre, chacun etant une vraie cause distincte : (1) **`.env.e2e.local` n'etait lu par personne** — aucun code ne le chargeait, le test se contentait de `test.skip()` en silence ; parseur inline ajoute dans `playwright.config.ts` (sans dependance dotenv, CRLF-safe, guillemets retires) avec une ligne de diagnostic affichee a CHAQUE lancement, qui a ensuite servi a isoler toutes les causes suivantes. (2) Mot de passe errone dans le fichier de l'utilisateur (donnee, pas code) — rendu visible en remontant le message d'erreur reel affiche sur `/auth`. (3) **Blocage de 30s sur `.textContent()`** : sans `{timeout}` explicite, Playwright attend l'apparition de l'element jusqu'au timeout global du test ; l'appel echouait donc en 30s au lieu de renvoyer `null` tout de suite. (4) **Placements de composition perdus en cours de boucle** (9/11 au lieu de 11/11) : les clics partaient plus vite que la mise a jour d'etat Vue, et un clic sur une position deja occupee sans joueur selectionne la DE-place (comportement voulu du tap-to-place) ; corrige en attendant le compteur "X/11 places" apres chaque paire de clics. (5) **Vraie cause du 0/11** : la popup "Reprendre le 11 du match precedent ?" (feature du 14/09) est un overlay plein ecran qui absorbait les premiers clics ; le test ne la fermait pas. Le premier correctif etait lui-meme bugge (`isVisible()` ne fait qu'un seul coup d'oeil immediat et ne reessaie pas, alors que la popup n'apparait qu'apres une requete reseau) — corrige avec `waitFor({state:'visible'})` avant le `isVisible()`. (6) **Timeout global du test laisse au defaut de 30s** : tous les `timeout: 30000` poses sur les assertions finales etaient donc inoperants, le test etait tue avant meme que l'IA commence a repondre (`timeout: 180000` dans `playwright.config.ts`), et le bouton "Analyser le match" n'etait en fait jamais clique (un `count()` immediat renvoyait 0 pendant le spinner de chargement). Selecteurs XPath `../..` des sections du rapport remplaces par des `data-testid` stables dans `ReportView.vue`. Deux `data-testid` ajoutes aussi dans `LineupView.vue` (slots du terrain, joueurs du banc).
- 15/09/2026 (suite) : **Le test e2e, une fois arrive au bout, a trouve un vrai bug de production** : `column events.player_in does not exist` a la generation du rapport IA. Cause — l'edge function `generate-report` deployee sur Supabase etait restee en **v16, d'avant la migration `20260911120000_players_and_lineups.sql`** (11/09/2026), qui a remplace les colonnes texte libre `player_in`/`player_out` par des references `player_in_id`/`player_out_id`. Le fichier local `supabase/functions/generate-report/index.ts` etait correct et a jour depuis cette migration (il resout meme les noms des joueurs via `players`), mais n'avait jamais ete redeploye — la generation de rapport IA etait donc cassee en production depuis le 11/09 sans que personne s'en apercoive, aucun test ne couvrant ce chemin jusqu'ici. **Version locale deployee en v17 par Claude via MCP, contenu redeploye relu et verifie identique au fichier local.** Bug latent signale au passage sans le corriger (voir Prochaines etapes) : la fonction filtre le match sur `created_by`, ce qui empeche un coach non-createur ou un tracker designe de generer le rapport.
- **Limite d'environnement confirmee (a ne pas reessayer)** : la sandbox de cette session distante ne peut pas atteindre `qsirpgbbovjcvduahyvy.supabase.co` (proxy de sortie qui repond `403` au CONNECT, confirme deux fois via `$HTTPS_PROXY/__agentproxy/status`). Impossible donc de lancer `npm run dev`/`npm run e2e` contre le vrai backend depuis ici, meme avec de vraies cles — d'ou les allers-retours avec l'utilisateur pour chaque execution. L'acces direct a la base et aux edge functions via MCP Supabase reste lui parfaitement disponible (c'est ce qui a permis de diagnostiquer et redeployer la v17).
- 15/09/2026 (fin) : **`npm run e2e` passe enfin de bout en bout** (`1 passed`, 22s), confirme par l'utilisateur apres le redeploiement de `generate-report` en v17. Le parcours complet de l'app (connexion, creation de match, composition des 11 titulaires, tracker, carton, fin de match, generation reelle du rapport IA avec ses 5 sections) est desormais couvert automatiquement — c'est le premier verrou de non-regression du projet, et il a paye immediatement en revelant que la generation de rapport IA etait cassee en prod depuis le 11/09. **Lecon a retenir : les migrations de schema de ce projet ne propagent pas les edge functions.** Toute migration qui renomme/supprime une colonne lue par `supabase/functions/*` exige un redeploiement explicite de la fonction concernee (via MCP `deploy_edge_function` ou `npx supabase functions deploy`) ; le fichier local a jour dans le depot ne garantit rien sur ce qui tourne reellement. A verifier systematiquement apres chaque migration touchant `events`, `matches`, `players` ou `reports`.
- 15/09/2026 (suite) : Correction du filtre `created_by` de `generate-report`, signale la veille comme bug latent. La fonction lisait le match via le client **admin** en le filtrant sur `.eq("created_by", user.id)` : tout membre qui n'avait pas cree le match lui-meme (coach, adjoint, responsable de categorie) recevait "Match introuvable" au moment de generer le rapport, alors que les policies RLS lui en donnent le droit. Fix : le match est desormais lu via le client **utilisateur** (donc soumis au RLS `can_access_team`), puis le droit d'ecriture est verifie explicitement par un RPC `can_write_team(club_id, team_id)` **avant** l'appel Anthropic (pour ne pas consommer un appel payant avant de se faire refuser l'ecriture par les policies de `reports`). Choix d'implementation : deleguer l'autorisation aux policies deja ecrites et testees plutot que de redupliquer la logique des 8 roles dans l'edge function. Deploye en **v18**, contenu redeploye relu et verifie identique au fichier local. **Verifie par simulation RLS** (transactions `BEGIN`/`ROLLBACK`, `set local role authenticated` + `request.jwt.claims`, aucune trace laissee), 4 scenarios sur des donnees reelles : (1) COACH rattache mais non-createur — l'ancien filtre `created_by` le refusait sur les 4 matchs testes, il peut desormais lire ET generer ; (2) DIRIGEANT (lecture seule) — lit le match mais ne peut pas generer, refus propre avant l'appel IA ; (3) OWNER d'un autre club — RLS masque completement la ligne, la fonction repond "Match introuvable" ; (4) OWNER createur (le cas exerce par le test e2e) — inchange, lit et genere.
- 15/09/2026 (suite) : Retours de l'utilisateur sur son 2e vrai test dans le navigateur (capture de la Home a l'appui). Trois points traites, plus un 4e trouve en creusant.
  - **"Les textes sont bizarres" — vrai bug, pas un choix de design** : `Inter` etait bien importe depuis Google Fonts dans `main.css`, mais **applique nulle part** (aucune regle `font-family`, aucun `--font-sans` defini). Toute l'app rendait donc dans la police systeme du navigateur (Segoe UI sous Windows) alors que le handoff NRV est dessine en Inter. Corrige par `--font-sans` + `font-family: var(--font-sans)` sur `body`. **Verifie dans un vrai navigateur** (serveur de dev + Chromium headless, lecture de `getComputedStyle`) : `body` et `font-data` resolvent desormais vers Inter, `font-score` reste sur Space Mono.
  - Choix tranche avec l'utilisateur sur les chiffres en monospace : `--font-score` (Space Mono) **conserve** pour les scores et gros compteurs — les chiffres y sont a chasse fixe, donc une colonne de scores reste alignee — mais `--font-data` (JetBrains Mono), qui produisait les petits compteurs et initiales (`5`, `3N`, `16`, `DY`), repasse en Inter : c'est lui qui donnait l'aspect "machine a ecrire". JetBrains Mono retire de l'import Google Fonts, plus aucun usage.
  - **HomeView, carte "Prochains matchs"** : jugee peu lisible par l'utilisateur, a raison — chaque match embarquait un bouton pleine largeur, donc 5 matchs = 5 gros blocs identiques. Passee en lignes compactes sur le modele de "Derniers resultats" (deja present juste en dessous et bien plus dense) ; seul le match le plus imminent garde un vrai bouton d'action.
  - **Le test e2e polluait la vraie base** (trouve en regardant la capture : 5 "E2E FC ..." dans les prochains matchs + 3 dans les resultats). Chaque `npm run e2e` creait un vrai match dans le club de production. 7 matchs de test supprimes en base (avec leurs 96 lignes de composition, 3 evenements et 1 rapport), et le test nettoie desormais apres lui via un `test.afterEach` qui supprime le match qu'il vient de creer. **Point de schema releve au passage** : `events` et `match_lineups` ont `on delete cascade` sur `match_id`, mais **pas `reports`** (`no action`) — le rapport doit donc etre supprime AVANT le match, sinon la suppression echoue. L'URL et la cle anon necessaires au nettoyage sont lues depuis `.env.local` (deja present pour Vite) par `playwright.config.ts`, donc rien de plus a renseigner a la main.
  - **LineupView, scroll toujours pas bon** (2e capture, le banc reduit a une fente avec une mini-scrollbar) : le terrain etait fige a `h-[380px]`, donc sur un ecran large mais peu haut il ne restait presque rien pour la liste des joueurs. Passee en 2 colonnes des `lg` a la demande de l'utilisateur (banc + actions a gauche sur 360px, terrain a droite qui prend toute la hauteur disponible), l'empilement vertical actuel etant conserve en mobile via `order`. **Non verifiable visuellement depuis cette session** (l'ecran exige un match reel et un compte connecte, et le reseau de la sandbox ne joint pas Supabase) — a valider par l'utilisateur.
- 16/09/2026 : L'utilisateur confirme en une fois plusieurs points en attente : les 6 corrections du 15/09/2026 (delegation tracker, suppression match, header ReportView, affordances Home) sont OK en conditions reelles ; la hierarchie de roles etendue testee reellement (PRESIDENT/CATEGORY_MANAGER/ADJOINT/DIRIGEANT) fonctionne ; le tracker designe qui ne peut pas generer le rapport est un comportement voulu, tranche definitivement (voir Decisions Prises) ; "Pages profil + permissions de suppression" recoit un feu vert de principe mais le perimetre reste a clarifier (le flux d'auto-inscription existe deja depuis le 15/09, donc peut-etre deja partiellement couvert).
- 16/09/2026 (suite) : Construction du bouton "modifier/retirer un membre", signale la veille comme le vrai trou du systeme d'invitation (`removeMember` existait dans le store mais n'etait appelee nulle part cote UI, et aucune fonction de modification n'existait). `clubMembers.store.ts` : nouvelle fonction `updateMember(id, {role, teamIds, categories})` qui remplace integralement les rattachements equipe/categorie (delete puis insert, plus simple qu'un diff pour un volume de quelques lignes par membre) et reutilise la meme validation que `inviteMember` (au moins une equipe/categorie selon le role). `MembersSection.vue` : le formulaire d'invitation devient partage creation/edition (`editingMember` state) — email masque et non modifiable en edition (il identifie le compte), reste du formulaire (role/equipes/categories) identique et pre-rempli. Boutons Modifier/Retirer sur chaque carte membre, visibles uniquement pour `isOwner` et jamais sur la ligne du OWNER lui-meme (l'edition/suppression de son propre acces aurait pu orpheliner le club). Popup de confirmation avant retrait, meme style que la suppression de match de la veille. Aucune migration necessaire : les policies RLS `club_members_update_owner`/`club_members_delete_owner` et `club_member_teams`/`club_member_categories` (insert/delete owner) couvraient deja ce besoin. **Verifie par simulation RLS** (transaction `BEGIN`/`ROLLBACK`, `set local request.jwt.claims`) : changement de role + remplacement des equipes d'un membre reel confirme, creation+suppression d'un membre bidon confirmee, aucune trace laissee. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore teste en conditions reelles.**
- 16/09/2026 (suite) : Clarification demandee sur "pages profil + permissions de suppression" (le flux d'auto-inscription magic-link existait deja, restait a savoir ce qui manquait). Reponse de l'utilisateur en 3 points, tranchant definitivement le sujet : un joueur qui modifierait ses propres infos serait plus deroutant qu'utile pour le coach (pseudo/nom non reconnu) — ecarte ; la suppression/le depart du club doit rester une action du coach, jamais du joueur lui-meme — confirme que le bouton "Retirer" construit plus tot dans la journee (owner-only) est la bonne forme, pas une action cote joueur a construire ; retirer un joueur de l'effectif est deja couvert par l'archivage existant. **Chantier cloture, rien a construire de plus.**
- 16/09/2026 (suite) : Ajout de `matches.is_home` a la demande de l'utilisateur ("le nom du club a gauche = l'equipe a domicile, geste elementaire au foot"). Plutot que d'essayer de deduire "a domicile" du texte libre `home_team` (source de l'imprecision existante, cf. TeamStatsView), ajout d'un choix explicite et decouple : toggle "Notre equipe joue : a domicile / a l'exterieur" dans `CreateMatchModal.vue` (defaut a domicile), stocke tel quel. `TeamStatsView.vue` n'approxime plus rien : utilise directement `m.isHome`. Migration `20260916100000_matches_is_home.sql` appliquee en prod avec un backfill best-effort sur les 5 matchs existants (meme heuristique texte que l'ancien code, seule donnee disponible retroactivement) — releve un cas ou elle se trompe deja ("FCF" au lieu de "FC Franconville", classe a tort en exterieur), a corriger manuellement par l'utilisateur si besoin. Verifie par simulation RLS (insert en transaction annulee) sur les donnees reelles du club. Build/typecheck valides (types `Match.isHome`, `match.store.ts`, `playerProfile.store.ts` mis a jour en meme temps).
- 16/09/2026 (suite) : Question de l'utilisateur sur Sentry ("j'ai deja fait ma part, VITE_SENTRY_DSN dans mon .env.local"). Verification du code : **le monitoring d'erreurs cote client est deja entierement construit et branche** (`@sentry/vue` en dependance, `Sentry.init()` dans `main.ts` avec `browserTracingIntegration`, conditionne a `VITE_SENTRY_DSN` — actif seulement si la variable est definie), decouvert par Claude en lisant le code plutot que documente jusqu'ici dans ce fichier (trou de tenue de CLAUDE.md corrige au passage). Le seul point encore ouvert n'est pas du code mais du deploiement : les variables Vite sont figees dans le bundle au moment du `npm run build` execute PAR LE VPS (`deploy.yml`), pas au runtime — donc `VITE_SENTRY_DSN` doit etre presente dans le `.env.local` DU VPS (`/srv/saas/nrv/.env.local`) au moment du build pour que Sentry s'active en prod, un `.env.local` sur la machine locale de l'utilisateur ne suffit pas. Question posee a l'utilisateur pour confirmer ce point precis. Non couvert : l'edge function `generate-report` ne remonte pas ses erreurs a Sentry (optionnel, non demande).
- 16/09/2026 (fin) : L'utilisateur confirme que `VITE_SENTRY_DSN` est present a la fois sur sa machine locale ET dans le `.env.local` du VPS — celui reellement lu par `npm run build` au moment du deploiement. Sentry est donc **deja actif en production**, aucune action supplementaire necessaire. Chantier cloture.
- 16/09/2026 (suite) : Retour de l'utilisateur en 3 points sur le message precedent. (1) Confirmation "c'est bon" pour le bouton modifier/retirer un membre. (2) **Recadrage ferme sur une speculation non verifiable** : Claude avait avance qu'un match historique precis ("FCF") etait "sans doute" a domicile — l'utilisateur a repondu "Non. Je vois pas comment tu pourrais savoir s'il s'agissait d'un match a domicile ou non. Reste a ta place, specule pas." Aucune donnee n'avait ete modifiee sur la base de cette remarque (verbale uniquement), mais retenu comme regle de conduite durable pour la suite de la session (ajoutee dans Blocages et Points d'Attention) : ne jamais affirmer un fait du monde reel non verifiable, demander ou stocker une donnee explicite a la place. (3) **Acces cross-equipe en lecture demande precisement** : "le coach de l'equipe 1 peut voir les stats de l'equipe 2 [...] mais pas l'inverse", avec une equipe 3 eventuelle suivant la meme regle. Implemente via une "equipe fanion" (`teams.is_flagship`, colonne booleenne + index unique partiel `teams_one_flagship_per_club` garantissant au plus une par club) : `can_access_team()` reecrite avec une nouvelle branche qui donne un acces LECTURE au COACH/ADJOINT de l'equipe fanion sur toutes les equipes du club (matchs/evenements/rapports/stats via les policies existantes qui s'appuient dessus) — `can_write_team()` n'est pas touchee, donc jamais d'ecriture cross-equipe, et aucune branche symetrique n'existe pour les equipes non-fanion (elles ne se voient jamais entre elles, ni ne voient la fanion). Migration `team_flagship_cross_read` **appliquee directement en prod par Claude**, verifiee par 2 simulations RLS en transaction annulee sur un coach de test reel : (a) une fois designe sur l'equipe fanion, il obtient bien `can_access_team=true`/`can_write_team=false` sur une autre equipe a laquelle il n'est pas rattache directement ; (b) sans etre sur l'equipe fanion, un coach reste bloque sur les equipes des autres — confirmant le sens unique demande. Cote client : `ClubTeam.isFlagship`, `teamsStore.setFlagshipTeam(clubId, teamId)` (retire le flag ailleurs dans le club avant de le poser, respecte la contrainte d'unicite), `TeamsSection.vue` affiche une icone etoile a cote du nom de l'equipe fanion et un bouton "Designer equipe fanion"/"Retirer le statut fanion" (owner-only) dans les actions de chaque equipe. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Point de clarification (4) verifie mais pas un bug a corriger** : l'utilisateur s'inquietait qu'un joueur "appele en renfort" dans une autre equipe puisse voir ses stats comptees en double ou perdues — verifie dans `playerProfile.store.ts` que ce n'est deja pas le cas : les stats sont calculees par `match_lineups` (une ligne par joueur par match, jamais dupliquee), chaque apparition cross-equipe est juste marquee par un flag `calledUp` sans jamais exclure ni dupliquer les stats du match concerne. Rien a changer, confirme a l'utilisateur. **Pas encore teste en conditions reelles** (necessite de designer une vraie equipe fanion et de se connecter avec son coach).
- 16/09/2026 (suite) : Nouvelle demande de l'utilisateur suite au test visuel du Tracker (capture d'ecran a l'appui, terrain ecrase) — 4 points traites en une fois. (1) **Promotion/Renfort** : "je veux qu'on comptabilise dans ses stats le fait qu'un joueur initialement dans une equipe inferieur, soit promu dans l'equipe superieure" avec badges distincts. Question posee d'abord (comment determiner quelle equipe est "au-dessus") : l'utilisateur a choisi le nouveau champ `teams.level` plutot qu'une deduction depuis `teams.category` (voir Decisions Prises). Implemente : migration `teams_level_hierarchy` (colonne nullable, champ dans le formulaire equipe de `TeamsSection.vue`), `playerProfile.store.ts` calcule desormais un `crossTeamStatus: 'PROMOTION' | 'RENFORT' | null` par match (comparaison des niveaux, fallback "RENFORT" si l'un des deux niveaux manque — comportement identique a l'ancien badge unique), `PlayerProfileView.vue` affiche les compteurs "Promotion 💫"/"Renfort 💪" + le badge par ligne d'historique. (2) **Nouvelles stats par poste** : demande de ne plus afficher "buts" pour un gardien, et d'ajouter ballons recuperes/intercepted/perdus (defenseurs/milieux) + tacles (defenseurs) + tirs tentes (attaquants). Question posee sur le mecanisme (l'utilisateur a confirme l'option d'attribuer un joueur aux evenements existants/nouveaux plutot que de se limiter aux tirs). Implemente : 4 nouveaux `EventType` (`BALL_WON`/`BALL_LOST`/`INTERCEPTION`/`TACKLE`, aucune migration necessaire — voir Decisions Prises), attribution du joueur aux tirs via une modale apres le tap terrain (comme pour un but), `CardPlayerModal.vue` generalise en `PlayerEventModal.vue` (utilise `eventMeta()` au lieu d'un libelle carton en dur) et reutilise pour cartons + nouveaux types. `src/lib/positionCategory.ts` classe le texte libre `players.position` par mots-cles pour filtrer les stats affichees sur le profil. (3) **Terrain ecrase** : hauteur mobile passee de 300px a 420px (`PitchMap.vue` root passe en `h-full`, la hauteur reelle est desormais controlee par le conteneur parent). (4) **Layout desktop** : "adopte le meme agencement que la vue precedente [Lineup] ... compo d'un cote et le terrain de l'autre" — `TrackerView.vue` restructure en 2 colonnes des `lg` (chrono/palette/timeline a gauche dans une colonne de largeur fixe, terrain a droite sur toute la hauteur disponible), meme structure Flexbox que `LineupView.vue`. `ReportView.vue` (chronologie du rapport) mis a jour au passage pour utiliser `eventMeta()` au lieu d'une table de libelles dupliquee, avec attribution generique du joueur a tout type d'evenement portant un `player_id`. Migration `teams_level_hierarchy` **appliquee directement en prod par Claude**. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides a chaque etape. **Pas encore teste en conditions reelles** (necessite de renseigner un niveau d'equipe et de saisir les nouveaux types d'evenements dans un vrai match).
- 16/09/2026 (suite) : L'utilisateur revient sur le champ "niveau" ajoute juste avant ("On va modifier la chose") et fournit la pyramide exacte des divisions francaises, du plus bas au plus haut : D5 a D1 (departemental), R3 a R1 (regional), N2-N1 (national), Ligue 3 a Ligue 1 (national et professionnel). Demande un select ferme pour la division a la creation d'equipe, dont la valeur doit etre reutilisee partout ou la division est mentionnee dans l'outil pour en deduire un classement. Implemente : `src/lib/divisions.ts` (liste fermee en 4 groupes, `divisionRank()` qui renvoie l'index = le rang), `teams.division` passe d'un `<input>` texte a un `<select>` avec optgroups dans `TeamsSection.vue`, et `teams.level` (ajoute le jour meme) **retire completement** — colonne supprimee (migration `drop_teams_level_use_division_rank`), champ formulaire retire, tout code associe nettoye. `PlayerProfileView.vue` calcule desormais les niveaux d'equipe via `divisionRank(team.division)` au lieu de `team.level`. Point d'attention corrige au passage : le sens de la comparaison Promotion/Renfort dans `playerProfile.store.ts` s'est inverse (`teams.level` avait 1 = equipe la plus haute, `divisionRank()` a l'inverse un rang plus eleve pour l'equipe la plus haute) — `matchLevel > currentLevel` au lieu de `<` pour designer une Promotion. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore teste en conditions reelles.**

- 16/09/2026 (suite) : L'utilisateur demande de retester tout en conditions reelles — rappele que cette session distante ne peut pas se connecter a une vraie session utilisateur authentifiee (limite d'environnement deja documentee plus haut). Avant ca, nouvelle demande precise : pour un match sans equipe exacte rattachee (ex. amical), la liste de joueurs proposee en composition (`LineupView.vue`) ne doit plus etre TOUT le club (un joueur U18 se retrouvait melange aux seniors) mais le groupe Senior par defaut, avec un ajout ponctuel explicite (petit lien -> choix de categorie -> cases a cocher joueur par joueur) si besoin d'inclure un joueur d'une autre categorie. Implemente : le filtrage par equipe exacte (cas normal, championnat) est inchange ; seul le cas `matches.team_id` null change de comportement — vivier par defaut = equipes dont `category` contient "senior" (regex insensible casse/accents, `teams.category` restant du texte libre), panneau "+ Inclure des joueurs d'une autre categorie" pour ajouter ponctuellement (stocke uniquement en memoire le temps de la composition, jamais persiste). Message explicite si le groupe Senior est vide (categorie "Senior" pas encore renseignee sur une equipe) plutot que de retomber silencieusement sur la liste complete. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore teste en conditions reelles.**
- 16/09/2026 (suite) : L'utilisateur confirme aller tester en conditions reelles et demande en parallele une popup de confirmation sur `ClaimProfileView.vue` (l'ecran ou un joueur invite relie son compte a une fiche effectif) : "Salut {prenom}, tu t'apprete a lier ton email a ce joueur, il s'agit bien de toi ?" — signale a raison qu'aucune securite n'existe pour empecher un clic distrait de lier son compte a la fiche d'un AUTRE joueur. Verifie dans `claim_player_profile()` (migration `20260915120000_player_self_claim_profile.sql`) qu'un compte ne peut reclamer qu'une seule fiche a vie (aucun RPC de dissociation) — confirme que l'operation merite bien une confirmation explicite. Implemente : clic sur une fiche ouvre desormais une popup recapitulative (prenom extrait de `player.name`, poste + numero affiches) avant d'appeler le RPC, au lieu de lier immediatement. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore teste en conditions reelles.**
- 16/09/2026 (suite) : Nouvelle demande, en attendant le retour de test de l'utilisateur : dans `CreateMatchModal.vue`, le toggle domicile/exterieur (`isHome`) place desormais automatiquement le nom du club dans le champ texte correspondant (Domicile si a domicile, Exterieur si a l'exterieur), plutot que de laisser les deux champs vides a completer par le coach. N'ecrase jamais un nom d'adversaire deja saisi de l'autre cote (ne touche le champ oppose que s'il contenait deja le nom du club). Applique au montage (valeur par defaut `isHome=true`) et a chaque bascule du toggle. Aucune migration, purement client. Build/typecheck valides. **Pas encore teste en conditions reelles.**
- 16/09/2026 (suite) : **Bug reel signale par l'utilisateur avec capture d'ecran** — un match amical cree en rattachant l'equipe "Équipe Première" n'affichait pas du tout le panneau "+ Inclure des joueurs d'une autre categorie" ajoute plus tot. Cause : la condition `!matchStore.currentMatch?.teamId` masquait le panneau des qu'une equipe etait precisee, alors que le vrai besoin de l'utilisateur (appeler un U18 pour un amical d'Équipe Première) survient justement quand une equipe EST precisee. Corrige : le panneau est desormais toujours affiche (`clubsStore.canWrite` seul), la categorie exclue de la liste "autres categories" proposee est celle du vivier de base (categorie de l'equipe du match si precisee, sinon "Senior") plutot que toujours "Senior" en dur. Reconnu explicitement a l'utilisateur que l'affirmation precedente ("c'est fait") etait fausse pour son cas d'usage reel — corrige immediatement plutot que de defendre l'implementation initiale. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore reteste en conditions reelles par l'utilisateur.**
- 16/09/2026 (suite) : **2e signalement sur le meme chantier, avec capture d'ecran** — le select "+ Inclure des joueurs d'une autre categorie" s'ouvrait mais restait vide (aucune option), sans aucune explication a l'utilisateur. Diagnostic confirme par l'utilisateur lui-meme : aucune equipe du club n'a `teams.category` renseigne (ni "Senior" ni rien d'autre), donc `otherCategories` etait structurellement vide — pas un bug de logique, un vrai manque de configuration que l'UI ne signalait pas. Ajout d'un repli explicite dans `LineupView.vue` : si `otherCategories` est vide, affiche directement tous les autres joueurs du club (hors vivier de base) a cocher un par un, avec un message explicatif + un raccourci vers `Mon club > Equipes` (`router.push({ name: 'club', query: { tab: 'teams' } })`) pour renseigner des categories et retrouver un tri plus fin la prochaine fois. Refactor au passage : `baseSquad`/`baseSquadIds` extraits en computed partages entre `squad` et le nouveau `otherPlayersFallback`. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore reteste en conditions reelles par l'utilisateur.**
- 16/09/2026 (suite) : **Vrai bug signale avec capture d'ecran** — en testant le fix precedent, l'utilisateur a navigue vers `/club?tab=teams` via le raccourci puis appuye sur "retour" (navigateur), et s'est retrouve directement sur `TrackerView.vue` (chrono 00:00, terrain vide, bouton "Demarrer") pour un match dont la composition n'etait pas terminee — sans aucun message, sans possibilite de revenir completer la composition. Cause confirmee en lisant le code : `TrackerView.vue` n'a jamais verifie que les 11 titulaires etaient places avant d'autoriser l'acces (ni avant, ni apres cette session) ; n'importe quelle navigation directe (retour navigateur, URL tapee, lien partage) pouvait y atterrir sur un match sans effectif. Corrige : `onMounted` compte desormais les entrees `role === 'STARTER'` de `lineupStore.entries` apres le chargement, et redirige vers `LineupView.vue` (`router.replace`, `query: { incomplete: '1' }`) si moins de 11 sont placees ET que l'utilisateur peut agir sur cette composition (`canTrack` — coach/adjoint/tracker designe) ; un lecteur seul garde l'acces direct deja voulu ("Voir le match" depuis Lineup, meme composition incomplete). `LineupView.vue` affiche desormais un bandeau ("Il manque des titulaires — place les 11 avant de suivre ce match en direct.") quand `route.query.incomplete === '1'`, avec le bouton "Place les 11 titulaires" deja existant juste en dessous pour terminer la composition sans avoir a redemander comment y revenir. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore reteste en conditions reelles par l'utilisateur.**
- 16/09/2026 (suite) : 2 demandes rapides de l'utilisateur pendant qu'il teste. (1) Changer de formation en cours de composition vidait completement `assign` (tous les joueurs deja places repartaient au banc, obligeant a tout replacer) — signale comme "tres chiant". `selectFormation()` redistribue desormais les joueurs deja places sur les postes de meme ligne (G/D/M/A) de la nouvelle formation, dans leur ordre actuel dans l'ancienne ; si la nouvelle formation a moins de postes sur une ligne (ex. 3 defenseurs en 3-5-2 contre 4 en 4-4-2), l'excedent repasse simplement sur le banc au lieu d'etre perdu — pas de garantie de placement identique, mais plus jamais de reset total. (2) Ajout des evenements PENALTY_FOR/PENALTY_AGAINST (offensif/defensif) a la palette du Tracker, meme traitement que corners/coups francs (tap terrain, pas d'attribution joueur) — aucune migration necessaire. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore reteste en conditions reelles.**
- 16/09/2026 (suite) : Demande de precision sur les penalties ajoutes juste avant : pour un penalty POUR nous, renseigner le tireur ET s'il a marque ou non ; un penalty marque doit directement compter comme un but pour l'equipe. Pour un penalty CONTRE nous, juste marque/non marque (pas de tireur, "vu que de toute facon on peut pas"). Implemente : migration `events_penalty_scored` (colonne `events.penalty_scored` boolean nullable, uniquement pertinente pour PENALTY_FOR/PENALTY_AGAINST). Nouvelle modale `PenaltyModal.vue` : pour PENALTY_FOR, select tireur (reutilise le champ `scorer_id`, deja utilise pour les buteurs) + toggle Marque/Non marque obligatoire ; pour PENALTY_AGAINST, uniquement le toggle. `TrackerView.vue` : les 2 types sont desormais des actions instantanees (pas de tap terrain, comme les cartons) qui ouvrent cette modale ; a la confirmation, si "marque", un **2e evenement reel** GOAL_FOR/GOAL_AGAINST est cree immediatement en plus du PENALTY_FOR/AGAINST (meme tireur pour le GOAL_FOR) — le score (`scoreHome`/`scoreAway`, deja bases sur le comptage GOAL_FOR/GOAL_AGAINST) et les stats de buts du profil joueur (deja basees sur `scorerId` d'un GOAL_FOR) restent donc corrects sans dupliquer de logique de comptage specifique aux penalties. `EventLog.vue` (Timeline du Tracker) et `ReportView.vue` (Chronologie du rapport) affichent desormais "Penalty marque/manque — {joueur}" pour ces 2 types. 3 copies dupliquees de `rowToEvent()` trouvees et mises a jour (`events.store.ts`, `playerProfile.store.ts`, `useMatchSync.ts` — la 3e, decouverte par l'erreur de typecheck qu'elle a declenchee). **Point non traite, signale pour reference future** : l'edge function `generate-report` (stats fournies au prompt IA) ne compte pas les penalties dans ses agregats (`goalsFor`/`shotsOnTarget`/etc. bases sur une liste de types codee en dur qui n'a jamais inclus PENALTY_FOR/AGAINST) — non corrige cette fois pour eviter d'elargir le chantier sans demande explicite, mais un but issu d'un penalty reste bien compte dans `goalsFor` puisque c'est un vrai evenement GOAL_FOR separe. Build (`npm run build`) et typecheck (`vue-tsc -b`) valides. **Pas encore teste en conditions reelles.**
